body {
  margin: 0;
  padding: 0;

  font-family: "Segoe", sans-serif;
  font-size: 18px;
  line-height: 24px;
  font-weight: 400;
  color: var(--black-main-text);
  text-decoration: none;

  background-color: var(--white-background-title-color);

  overflow-x: hidden;
  box-sizing: border-box;
}

body *,
*::after,
*::before {
  box-sizing: inherit;
}
